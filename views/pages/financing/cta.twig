<section id="cta" class="cta">
    <div class="cta__wrapper">
		<h2>{{ post.get_field('cta_title') }}</h2>
		<a href="post.get_field('cta_button').url" class="ph-button">{{ post.get_field('cta_button').title }}</a>
    </div>
</section>
